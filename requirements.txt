customtkinter>=5.2
Pillow>=10.0

# Playback. python-vlc needs VLC itself installed too - see the Setup
# section. sounddevice is what lets the built-in player drive the audio
# device directly, which is what keeps its picture locked to its sound;
# without it sound goes through a separate ffplay and drifts.
python-vlc>=3.0
sounddevice>=0.4
numpy>=1.24
